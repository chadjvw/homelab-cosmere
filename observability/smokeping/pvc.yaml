apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smokeping-config-nvmeof
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: org.democratic-csi.nvmeof
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: smokeping-data-nvmeof
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
  storageClassName: org.democratic-csi.nvmeof
