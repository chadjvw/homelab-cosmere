apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: dcgm
  namespace: flux-system
spec:
  interval: 1h
  path: ./observability/dcgm
  prune: true
  sourceRef:
    kind: OCIRepository
    name: default
  dependsOn:
    - name: repos
    - name: prometheus
