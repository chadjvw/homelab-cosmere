apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: readmeabook
  namespace: flux-system
spec:
  interval: 1h
  path: ./apps/readmeabook
  prune: false # re-enable after HelmRelease is healthy
  sourceRef:
    kind: OCIRepository
    name: default
  dependsOn:
    - name: repos
    - name: external-secrets
    - name: democratic-csi-nvmeof
    - name: cloudnative-pg
