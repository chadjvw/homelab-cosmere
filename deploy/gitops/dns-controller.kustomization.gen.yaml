apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
    name: dns-controller
    namespace: flux-system
spec:
    force: true
    interval: 1m
    path: components/dns-controller
    prune: true
    sourceRef:
        kind: OCIRepository
        name: default
    targetNamespace: default
