apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dawarich-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: org.democratic-csi.nvmeof
  resources:
    requests:
      storage: 20Gi
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dawarich-valkey-data
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: org.democratic-csi.nvmeof
  resources:
    requests:
      storage: 1Gi
