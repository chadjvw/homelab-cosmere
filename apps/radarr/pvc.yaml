apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: radarr-config-nvmeof
  namespace: default
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 2Gi
  storageClassName: org.democratic-csi.nvmeof
