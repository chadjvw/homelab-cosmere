apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: smokeping
  namespace: flux-system
spec:
  interval: 1h
  targetNamespace: default
  releaseName: smokeping
  chart:
    spec:
      chart: app-template
      version: 4.6.2
      sourceRef:
        kind: HelmRepository
        name: bjw-s
        namespace: flux-system
      interval: 12h
  values:
    controllers:
      main:
        containers:
          main:
            env:
              PGID: 1000
              PUID: 1000
              TZ: America/Denver
            image:
              repository: ghcr.io/linuxserver/smokeping
              tag: "2.9.0"
    persistence:
      config:
        existingClaim: smokeping-config
      data:
        existingClaim: smokeping-data
    service:
      main:
        controller: main
        type: LoadBalancer
        externalTrafficPolicy: Local
        ports:
          http:
            port: 80
    route:
      main:
        hostnames:
          - smokeping.int.vanwyhe.xyz
          - smokeping.i.vw4.lol
          - smokeping.int.vw4.lol
        parentRefs:
          - group: gateway.networking.k8s.io
            kind: Gateway
            name: internal
            namespace: kube-system
            sectionName: https
